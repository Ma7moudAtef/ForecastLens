# Runtime dependencies — used by Streamlit Community Cloud (it prefers
# requirements.txt over pyproject.toml, and would otherwise run poetry on
# pyproject.toml and choke on the Windows-only pyinstaller extra).
# Keep in sync with [project.dependencies] in pyproject.toml —
# tests/unit/test_requirements_sync.py enforces the match.
#
# streamlit floor: 1.51 is the first release where st.plotly_chart accepts
# width="stretch" (st.dataframe/st.data_editor got it in 1.49, st.navigation
# in 1.36). Below 1.51 every chart raises TypeError. Verified against the
# published wheels; guarded by tests/unit/test_requirements_sync.py.
pandas>=2.1
numpy>=1.26
statsmodels>=0.14
scipy>=1.11
joblib>=1.3
openpyxl>=3.1
streamlit>=1.51
plotly>=5.18
pydantic>=2.5
